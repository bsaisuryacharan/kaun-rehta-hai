# Voter Lookup

Static voter lookup app generated from `/Users/bsaisuryacharan/Downloads/973.xlsx`.

## Run locally

```sh
cd /Users/bsaisuryacharan/Downloads/report/voter-lookup
python3 -m http.server 4173
```

Open `http://localhost:4173`.

## Current temporary public URL

`https://loose-kings-cough.loca.lt`

This URL depends on the local Python server and localtunnel process staying open on this Mac.
The app can also open directly from `index.html` because the data is bundled in `data.js`.
For a permanent free URL, deploy this folder to GitHub Pages, Netlify, Vercel, or Cloudflare Pages from an account you control.
